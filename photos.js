/* Photos for individual places.
 *
 * Key = the OpenStreetMap id shown at the bottom of a place's detail panel,
 * for example "node/1234567" or "way/7654321".
 * Value = a list of image paths or URLs, shown top to bottom in the panel.
 *
 * Put image files in the attachments/ folder and reference them like
 * "attachments/kfc-abovyan.jpg".
 *
 * These must be photos you took or otherwise have the right to publish.
 * Images from Google Maps or Yandex Maps are owned by them or their
 * contributors and cannot be copied here — the panel links out to those
 * services instead, so their photos stay on their own sites.
 */

const CHISHIKUNEM_PHOTOS = {
  // "node/1234567": ["attachments/example-outside.jpg", "attachments/example-inside.jpg"],
};
